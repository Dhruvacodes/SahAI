"""Protocol-engine fixture tests.

Each fixture file in this package contains positive and negative cases for a
single rule pack. The shared helper in ``conftest.py`` evaluates the engine
against a `VisitInput` and asserts which rules should/should not fire.
"""
